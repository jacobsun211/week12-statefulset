apiVersion: v1
kind: Service
metadata:
  name: coordinates-api-svc
  labels:
    app: api
spec:
  selector:
    app: api
  type: NodePort 
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080